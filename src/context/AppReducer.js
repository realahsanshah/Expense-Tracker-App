export default (state,action)=>{
    switch(action.type){
        
        case 'ADD_TRANSACTION':
            return state;

        case 'REMOVE_TRANSACTION':
            return state;

        default:
            return state;
    }
}